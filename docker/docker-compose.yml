
#
# WARNING you can build with register http://your-id.m.daocloud.io
#

# database
de-mysql:
  image: mysql:latest
  ports:
    # Allow client to access 3306
    - "3307:3306"
  privileged: true
  volumes:
    # NOTE: your data will be stored in ./mysql
    - ./mysql/data:/var/lib/mysql
    # Configs
    - ./mysql/my.cnf:/etc/mysql/my.cnf
    # Logs
    #- ./mysql/logs:/var/log/mysql
  environment:
    - MYSQL_ROOT_PASSWORD=123456 # you mysql password

# php-fpm
de-fpm:
  build: ./php
  ports:
    - "9001:9000"
  volumes:
    # For source code
    - ../app:/web/app
    - ../project:/web/project
    # Your php.ini
    - ./php/php.ini:/usr/local/etc/php/php.ini
  # environment:
    # Set your APP env variables here:
    # - APP_KEY=
    # - DB_HOST=
    # - DB_DATABASE=
    # - DB_USERNAME=
    # - DB_PASSWORD=
  links:
    - de-mysql:mysql

# web server
de-nginx:
  image: nginx:latest
  ports:
    - "81:80"
    - "444:443"
  volumes:
    # For others file
    - ../app:/web/app
    - ../project:/web/project
    # Configs
    - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    - ./nginx/conf.d:/etc/nginx/conf.d
    # Logs
    - ./nginx/logs:/var/log/nginx
    # Certificates
    #- ./nginx/cert/xxx.pem/:/etc/nginx/xxx.pem
    #- ./nginx/cert/xxx.key/:/etc/nginx/xxx.key
  links:
    - de-fpm:fpm
